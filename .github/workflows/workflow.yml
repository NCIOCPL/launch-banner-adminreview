name: Workflow
on:
  push:
    branches:
      - master
      - main
      - develop
      - 'hotfix/**'
      - 'release/**'
      - 'feature/**'
      - 'prototype/**'
    tags:
      - '*'
  pull_request:

jobs:
  ci_remote:
    permissions:
      contents: read
      packages: read # For npm install from GH Packages
      pull-requests: write  # For adding comments to PRs
    uses: nciocpl/launchables/.github/workflows/workflow.yml@ticket/1-workflow
    secrets: inherit
